package util;

import java.io.File;
import java.nio.file.Files;
import java.nio.file.Paths;

public class TestCaseFileGenerator {
    public void generateIntArray(int size, int lower, int upper) throws Exception {
//        File file = Files.createTempFile(Paths.get(System.getProperty("java.io.tmpdir")).getFileName());

    }
}
